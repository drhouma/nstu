#pragma once

#include <string>

// Find the longest common subsequence of two strings.
std::string lcs(const std::string& first, const std::string& second);
std::string Naiv(std::string One, std::string Two);